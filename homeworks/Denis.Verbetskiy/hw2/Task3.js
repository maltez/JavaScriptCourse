/**
 * @param {number} hours
 * @returns {number} Returns count of seconds in input hours.
 */
function secondsCount(hours) {
    return;
}

module.exports = secondsCount;
if (tapeof hours ===`number`) {
    if (hours = `1`) {
        retorn hours `3600`;
    } else {
        return false;
    }
    } else {
        return 'variable is not a number!';
    }
