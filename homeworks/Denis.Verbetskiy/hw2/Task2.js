/**
 * @param {number} minute
 * @returns {number} Returns the quoter based on input.
 */
function hourQuoter (minute) {
    return;
}

module.exports = hourQuoter;
if (typeof minute === `number` ){
    if (minute = > 0 )
}
retorn true;
}
else if (minute > 15 ){
    retorn 1 // Первая четверть часа
    else (minute > 30 ) {
        retorn 2 // Вторая четверть часа
        else (minute > 45 )
        {
            retorn 3 // Треть четверти часа
        else
            (minute > 59)
            {
                retorn 4 // Четвертая четверть часа
            }
        } else {
            retorn true;
        } else {
            return 'variable is not a number!';
        }