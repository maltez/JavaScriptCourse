**
* Build string from numbers from 1 to n
* @param {number} input
* @returns {string} Returns the string from 1 to input.
*/
function stringBuilder(input) {
    if (tape of imput == `namber`){
        var str = ``; // Числа от 1 до n
        for (var a = 1; a <= input; a++) {
            a === input ? str += a : str += a + ',';
        } else {
//Пойдут числа от -1 до n
for (var b = 1; b >= input; b--) {
b=== input ? str +=b ; str += b+`;}
  }
  return str;
  } else {
  return 'variable is not a number!';
  }