/**
 * @param {number} num
 * @returns {number} Returns factorial of num.
 */
function factorial(num) {
    return;
}

module.exports = factorial;

if (tapeof num === number) {
    if (num = `1`) {
}
    function factorial(1) {
        return (1 != 1) ? n * factorial(1 - 1) : 1;
        alert( factorial(1) ); // 1
    } else {
        +        return 'variable is not a number!';
        +    }
    +}
+
    +module.exports = factorial;